{
  "name": "current-delivery",
  "private": true,
  "workspaces": [
    "current-delivery-frontend",
    "current-delivery-backend"
  ],
  "scripts": {
    "start:frontend": "cd current-delivery-frontend && npm install && npm run build && npx serve -s build",
    "start:backend": "cd current-delivery-backend && npm install && npm start",
    "start": "concurrently \"npm run start:frontend\" \"npm run start:backend\"",
    "build:frontend": "cd current-delivery-frontend && npm install && npm run build",
    "build:backend": "cd current-delivery-backend && npm install && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "serve": "^14.2.0"
  }
}
